import { defineConfig } from 'astro/config';

export default defineConfig({
  // Replace with YOUR actual GitHub URL
  site: 'https://alexnikiforov.github.io', 
  // If your repo is NOT your-username.github.io, add the repo name here:
  base: '/intwocap', 
});